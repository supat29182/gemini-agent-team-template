# Core API Contract (Consolidated)
# This file is the consolidated main API Contract for the entire project.
# PM will consolidate from feature-level api_contract.yaml to here after Phase 3 ends.
#
# Format: OpenAPI 3.x
openapi: "3.0.3"
info:
  title: "Project API Contract"
  version: "0.0.1"
  description: "Consolidated API contract — auto-populated after feature delivery"
paths: {}
