{
  "brain-linter-and-gitnexus": {
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "npx prettier --write 'second-brain/**/*.md' '.agents/**/*.md' > /dev/stderr; echo '{}'",
            "timeout": 30
          },
          {
            "type": "command",
            "command": "python3 scripts/brain_linter.py --changed-only > /dev/stderr; echo '{}'",
            "timeout": 30
          },
          {
            "type": "command",
            "command": "python3 scripts/log_rotation.py > /dev/stderr; echo '{}'",
            "timeout": 15
          }
        ]
      }
    ]
  }
}
